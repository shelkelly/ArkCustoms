<div class="row">
    <!-- Portfolio Grid-->
    <section class="page-section bg-light" id="portfolio">
        <div class="container">
            <div class="text-center">
                <h2 class="section-heading text-uppercase">Product</h2>
                <h3 class="section-subheading text-muted">See the details!</h3>
            </div>
            <div class="row">
                <div class="col-lg">

                    {{#each customproduct}}

                    <div class="portfolio-item" data-id={{this.id}}>
                        <a class="portfolio-link" data-toggle="modal" href="#portfolioModal1">

                            <img class="img-fluid" src="{{this.imgurl}}" alt="" />
                        </a>
                        <div class="portfolio-caption">
                            <div class="portfolio-caption-heading">{{this.product}}</div>
                            <div class="portfolio-caption-subheading text-muted">{{this.this.price}}</div>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-lg-8 mx-auto text-center" id="itemInfoOptions">
                        <h4 id="productName">{{this.product}}</h4>
                        <p class="large text-muted" id="productDescription">{{this.description}}</p>
                    </div>
                </div>

                {{/each}}


            </div>
        </div>
</div>
</section>

</div>